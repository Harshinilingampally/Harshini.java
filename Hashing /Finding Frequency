/**
Input Format

The first line of input contains N - size of the array. The next line contains N integers, the elements of the array. The next line contains Q - number of queries. Each of the next Q lines contains a single integer X, for which you have to find the frequency of X in the given array.

Output Format

For each query, print the frequency of X, separated by a new line.
**/
import java.io.*;
import java.util.*;
public class Main {
    public static int frequencyFinder(Set<Integer> hs,int x){
     
       return hs.Collections.frequency(new ArrayList<>(hs),x);  
    }
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int N=sc.nextInt();
        int [] arr=new int[N];
        for(int i=0;i<N;i++){
        arr[i]=sc.nextInt();
        }
        int Q=sc.nextInt();
      HashSet<Integer,Integer> hs=new HashSet<Integer,Integer>();
      for(int items:arr){
          hs.add(items);
      }
        while(Q-->0){
         int X=sc.nextInt(); 
       int frequency=frequencyFinder(hs,X);
      System.out.println(frequency);
        }
    }
}











