public class Solution {
    
    public int singleNumber(final int[] A) {
        int N=A.length;
        int XOR=0;
        for(int i=0;i<N;i++){
            XOR=XOR^A[i];
        }
        
       return XOR; 
        
        
    }
}
