class Solution {
    public boolean isPossibleToSplit(int[] nums) {
        int [] countArray=new int[101];
        for(int item:nums){
            countArray[item]++;
            if(countArray[item] > 2) return false;
        }

         return true;
    }
}
